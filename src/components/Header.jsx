import React from "react";
import "./Header.css"; // Import styles for the header

const Header = () => {
  return (
    <header className="header">
      <h1 className="header-title">Worker Management System</h1>
    </header>
  );
};

export default Header;
